
Youre Edge Monorepo
- Root uses npm workspaces (web + mobile)
- web: Create React App style project (src, public). Run via `cd web && npm install && npm start`
- mobile: Expo project. Run via `cd mobile && npm install && npx expo start`

Mock API for web: public/mock/scores.json
