# Where the FastAPI engine is listening. Copy to .env.local to override the default
# (http://localhost:8000) — useful when running a second engine off a git worktree.
ENGINE_URL=http://localhost:8000
